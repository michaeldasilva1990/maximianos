# Auto detect text files and perform LF normalization
* text=auto

# Arquivos executados no servidor Linux: sempre LF, nunca CRLF.
# Um \r no fim da linha quebra o bash com "$'\r': command not found".
*.sh            text eol=lf
*.conf          text eol=lf
Jenkinsfile     text eol=lf
